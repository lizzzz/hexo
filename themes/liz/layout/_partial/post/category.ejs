<% if (post.categories && post.categories.length > 0){ %>
  <%post.categories.forEach(function(category){ %>    
  	<span><a href="<%-config.root%><%-category.path%>"><%= category.name %></a></span>
  <%  
  });
  %>
<% } %>